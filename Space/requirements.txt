# ═══════════════════════════════════════════════════════════════════════════════
# VieTrans Hugging Face Space — Requirements
# Gradio SDK + ZeroGPU
# ═══════════════════════════════════════════════════════════════════════════════

# ─── Gradio + HuggingFace Spaces GPU ─────────────────────────────────────────
gradio>=5.0.0
spaces>=0.28.0
pydantic==2.10.6

# ─── Core Image Processing ────────────────────────────────────────────────────
Pillow>=10.0.0
numpy>=1.24.4
opencv-python-headless>=4.8.0

# ─── Deep Learning (PyTorch – GPU via ZeroGPU) ───────────────────────────────
torch>=2.1.0
torchvision>=0.16.0
accelerate>=0.26.0

# ─── NLP / Translation (NLLB via HuggingFace) ────────────────────────────────
transformers>=4.40.0
tokenizers>=0.19.0
safetensors>=0.4.0
sentencepiece>=0.1.99
huggingface_hub>=0.25.1,<1.0.0
wordfreq>=3.1.1

# ─── OCR (PaddleOCR PP-OCRv5) ────────────────────────────────────────────────
# Paddle CPU is installed separately because the GPU wheel is Linux-CUDA only.
# ZeroGPU only allocates GPU for the NLLB translation step; PaddleOCR runs CPU.
paddleocr>=3.0.0
paddlepaddle>=3.0.0

# ─── Utilities ────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
tqdm>=4.66.0
orjson>=3.9.0
