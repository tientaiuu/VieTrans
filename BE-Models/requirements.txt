# ═══════════════════════════════════════════════════════════════════════════════
# VieTrans Requirements — Modified-DebackX Pipeline
# ═══════════════════════════════════════════════════════════════════════════════

# ─── Core Image Processing ────────────────────────────────────────────────────
Pillow>=10.0.0
numpy>=1.24.4
opencv-python-headless>=4.10.0
tqdm>=4.66.0

# ─── Deep Learning (PyTorch) ──────────────────────────────────────────────────
# For GPU: install torch with CUDA wheels separately, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# For CPU-only:
torch>=2.1.0
sentencepiece>=0.2.0

# ─── NLP / Translation (NLLB via HuggingFace) ────────────────────────────────
transformers>=4.44.0
tokenizers>=0.19.0
safetensors>=0.4.0
huggingface_hub>=0.22.0

# ─── OCR (EasyOCR — cross-platform, CPU/GPU compatible) ─────────────────────
# Note: PaddleOCR was evaluated but has a known incompatibility with
# PaddlePaddle 3.x on Windows (oneDNN bug). EasyOCR is used instead.
easyocr>=1.7.0

# ─── FastAPI Server ───────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# ─── Auth dependencies ───────────────────────────────────────────────────────
motor>=3.3.0
dnspython>=2.4.0
bcrypt>=4.0.0
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0
pydantic[email]>=2.0.0
fastapi-mail>=1.4.0
python-dotenv
gradio-client>=1.3.0

